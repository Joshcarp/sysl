Simple[package="simple"]:
    @basePath = "simple"

    /stuff:
        GET:
            return ok <: Stuff

    !type Stuff:
        Content <: string:
            @json_tag = "Content"  
    !type Foo:
        Content <: int
    /foobar:
        GET:
            return no <: Foo

syntax = "proto3";
package example;

message StringMessage {
    string value = 1;
}

service YourService {
    rpc Echo(StringMessage) returns (StringMessage) {
    }
}

service YourSecondService {
    rpc Echo(StringMessage) returns (StringMessage) {
    }
}

